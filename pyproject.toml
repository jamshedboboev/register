[project]
name = "register"
version = "0.1.0"
description = ""
authors = []
requires-python = ">=3.12"
dependencies = [
    "fastapi[standart] (>=0.136.1,<0.137.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
